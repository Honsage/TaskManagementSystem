openapi: "3.0.0"
info:
  title: Task Management API
  version: 0.9.0
  license:
    name: MIT
servers:
  - url: http://localhost:8080/
paths:
  /tasks:
    get:
    post:
  /tasks/{taskId}:
    get:
    put:
    delete:
  /tasks/{taskId}/start:
    post:
  /tasks/{taskId}/complete:
    post:
components:
  schemas:
    Task:
    Tasks:
    Error:
